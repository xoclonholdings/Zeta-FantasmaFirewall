import { storage } from "../storage";
export class ZenaIntegrityCore { async getStatus(ownerId: string) { const [findings, states] = await Promise.all([storage.getIntegrityFindings(ownerId, 100), storage.getExecutionStateCounts(ownerId)]); const unresolved = findings.filter((f) => !["RESOLVED", "ACCEPTED_RISK", "FALSE_POSITIVE"].includes(f.state)); const uncertainExecutions = states.filter((s) => ["PARTIAL", "UNKNOWN", "RUNNING"].includes(s.state)).reduce((sum, item) => sum + item.count, 0); return { identity: { galaxy: "ZENA", console: "Control", desk: "Integrity", surfaces: ["Logs", "Diagnostics", "Monitoring"] }, posture: unresolved.some((f) => f.severity === "CRITICAL") || uncertainExecutions ? "ATTENTION" : "CLEAR", unresolvedFindings: unresolved.length, criticalFindings: unresolved.filter((f) => f.severity === "CRITICAL").length, uncertainExecutions }; } }
export const zenaIntegrityCore = new ZenaIntegrityCore();
