import { storage } from "../storage";
export class FirewallService { private counters = { aiInjection: 0, corporateSabotage: 0, marketManipulation: 0, totalSignals: 0 }; async detectThreat(source: string, target: string, threatType: string) { const pattern = (await storage.getThreatPatterns()).find((p) => p.patternType === threatType); if (!pattern || pattern.confidence <= 80) return false; await storage.createSecurityEvent({ eventType: threatType, severity: ["AI_INJECTION", "CORPORATE_SABOTAGE"].includes(threatType) ? "CRITICAL" : "HIGH", source, target, description: `${threatType} signal matched from ${source} toward ${target}`, metadata: { patternId: pattern.id, confidence: pattern.confidence }, status: "ACTIVE" }); this.counters.totalSignals++; if (threatType === "AI_INJECTION") this.counters.aiInjection++; if (threatType === "CORPORATE_SABOTAGE") this.counters.corporateSabotage++; if (threatType === "MARKET_MANIPULATION") this.counters.marketManipulation++; return true; } getThreatCounters() { return this.counters; } }
export const firewallService = new FirewallService();
