[build]
builder = "nixpacks"

[deploy]
restartPolicyType = "on-failure"
restartPolicyMaxRetries = 10

[networking]
domains = []

[env]
NODE_ENV = "production"

[[services]]
name = "zena-control"
source = "."

[services.build]
buildCommand = "npm run build"
startCommand = "npm start"

[services.deploy]
port = 5000
healthcheckPath = "/api/firewall/public-status"
healthcheckTimeout = 30
